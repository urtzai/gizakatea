CREATE VIEW kilometro_1 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =1;
CREATE VIEW kilometro_2 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =2;
CREATE VIEW kilometro_3 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =3;
CREATE VIEW kilometro_4 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =4;
CREATE VIEW kilometro_5 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =5;
CREATE VIEW kilometro_6 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =6;
CREATE VIEW kilometro_7 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =7;
CREATE VIEW kilometro_8 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =8;
CREATE VIEW kilometro_9 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =9;
CREATE VIEW kilometro_10 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =10;
CREATE VIEW kilometro_11 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =11;
CREATE VIEW kilometro_12 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =12;
CREATE VIEW kilometro_13 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =13;
CREATE VIEW kilometro_14 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =14;
CREATE VIEW kilometro_15 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =15;
CREATE VIEW kilometro_16 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =16;
CREATE VIEW kilometro_17 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =17;
CREATE VIEW kilometro_18 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =18;
CREATE VIEW kilometro_19 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =19;
CREATE VIEW kilometro_20 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =20;
CREATE VIEW kilometro_21 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =21;
CREATE VIEW kilometro_22 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =22;
CREATE VIEW kilometro_23 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =23;
CREATE VIEW kilometro_24 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =24;
CREATE VIEW kilometro_25 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =25;
CREATE VIEW kilometro_26 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =26;
CREATE VIEW kilometro_27 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =27;
CREATE VIEW kilometro_28 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =28;
CREATE VIEW kilometro_29 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =29;
CREATE VIEW kilometro_30 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =30;
CREATE VIEW kilometro_31 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =31;
CREATE VIEW kilometro_32 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =32;
CREATE VIEW kilometro_33 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =33;
CREATE VIEW kilometro_34 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =34;
CREATE VIEW kilometro_35 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =35;
CREATE VIEW kilometro_36 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =36;
CREATE VIEW kilometro_37 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =37;
CREATE VIEW kilometro_38 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =38;
CREATE VIEW kilometro_39 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =39;
CREATE VIEW kilometro_40 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =40;
CREATE VIEW kilometro_41 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =41;
CREATE VIEW kilometro_42 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =42;
CREATE VIEW kilometro_43 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =43;
CREATE VIEW kilometro_44 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =44;
CREATE VIEW kilometro_45 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =45;
CREATE VIEW kilometro_46 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =46;
CREATE VIEW kilometro_47 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =47;
CREATE VIEW kilometro_48 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =48;
CREATE VIEW kilometro_49 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =49;
CREATE VIEW kilometro_50 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =50;
CREATE VIEW kilometro_51 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =51;
CREATE VIEW kilometro_52 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =52;
CREATE VIEW kilometro_53 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =53;
CREATE VIEW kilometro_54 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =54;
CREATE VIEW kilometro_55 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =55;
CREATE VIEW kilometro_56 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =56;
CREATE VIEW kilometro_57 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =57;
CREATE VIEW kilometro_58 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =58;
CREATE VIEW kilometro_59 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =59;
CREATE VIEW kilometro_60 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =60;
CREATE VIEW kilometro_61 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =61;
CREATE VIEW kilometro_62 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =62;
CREATE VIEW kilometro_63 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =63;
CREATE VIEW kilometro_64 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =64;
CREATE VIEW kilometro_65 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =65;
CREATE VIEW kilometro_66 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =66;
CREATE VIEW kilometro_67 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =67;
CREATE VIEW kilometro_68 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =68;
CREATE VIEW kilometro_69 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =69;
CREATE VIEW kilometro_70 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =70;
CREATE VIEW kilometro_71 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =71;
CREATE VIEW kilometro_72 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =72;
CREATE VIEW kilometro_73 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =73;
CREATE VIEW kilometro_74 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =74;
CREATE VIEW kilometro_75 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =75;
CREATE VIEW kilometro_76 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =76;
CREATE VIEW kilometro_77 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =77;
CREATE VIEW kilometro_78 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =78;
CREATE VIEW kilometro_79 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =79;
CREATE VIEW kilometro_80 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =80;
CREATE VIEW kilometro_81 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =81;
CREATE VIEW kilometro_82 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =82;
CREATE VIEW kilometro_83 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =83;
CREATE VIEW kilometro_84 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =84;
CREATE VIEW kilometro_85 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =85;
CREATE VIEW kilometro_86 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =86;
CREATE VIEW kilometro_87 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =87;
CREATE VIEW kilometro_88 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =88;
CREATE VIEW kilometro_89 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =89;
CREATE VIEW kilometro_90 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =90;
CREATE VIEW kilometro_91 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =91;
CREATE VIEW kilometro_92 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =92;
CREATE VIEW kilometro_93 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =93;
CREATE VIEW kilometro_94 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =94;
CREATE VIEW kilometro_95 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =95;
CREATE VIEW kilometro_96 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =96;
CREATE VIEW kilometro_97 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =97;
CREATE VIEW kilometro_98 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =98;
CREATE VIEW kilometro_99 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =99;
CREATE VIEW kilometro_100 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =100;
CREATE VIEW kilometro_101 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =101;
CREATE VIEW kilometro_102 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =102;
CREATE VIEW kilometro_103 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =103;
CREATE VIEW kilometro_104 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =104;
CREATE VIEW kilometro_105 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =105;
CREATE VIEW kilometro_106 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =106;
CREATE VIEW kilometro_107 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =107;
CREATE VIEW kilometro_108 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =108;
CREATE VIEW kilometro_109 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =109;
CREATE VIEW kilometro_110 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =110;
CREATE VIEW kilometro_111 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =111;
CREATE VIEW kilometro_112 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =112;
CREATE VIEW kilometro_113 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =113;
CREATE VIEW kilometro_114 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =114;
CREATE VIEW kilometro_115 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =115;
CREATE VIEW kilometro_116 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =116;
CREATE VIEW kilometro_117 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =117;
CREATE VIEW kilometro_118 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =118;
CREATE VIEW kilometro_119 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =119;
CREATE VIEW kilometro_120 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =120;
CREATE VIEW kilometro_121 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =121;
CREATE VIEW kilometro_122 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =122;
CREATE VIEW kilometro_123 as SELECT user_id, km, user_estado, lon_gps, lat_gps  FROM `ged_users` LEFT JOIN `ged_ibilbidea_puntuak` on (km = user_pk and user_tra_zen = tra_zen ) where user_pk =123;

